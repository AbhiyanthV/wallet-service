# Copy this file to .env and adjust as needed.
# docker-compose picks up .env automatically — no container rebuild required.

POSTGRES_DB=walletdb
POSTGRES_USER=wallet
POSTGRES_PASSWORD=wallet
POSTGRES_PORT=5432

SERVER_PORT=8080
DB_POOL_SIZE=20
